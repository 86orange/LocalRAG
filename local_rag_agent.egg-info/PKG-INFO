Metadata-Version: 2.4
Name: local-rag-agent
Version: 0.1.0
Summary: 完全本地运行的 RAG 知识库 AI Agent，支持自然语言与本地文档对话
Requires-Python: >=3.12
Description-Content-Type: text/markdown
License-File: LICENSE
Requires-Dist: pymupdf>=1.28.0
Requires-Dist: chromadb>=1.5.0
Requires-Dist: ollama>=0.4.0
Requires-Dist: python-docx>=1.1.0
Requires-Dist: streamlit>=1.59.0
Dynamic: license-file

# LocalRAG

完全本地运行的 RAG（检索增强生成）知识库 AI 助手。

让你的文档会说话——用自然语言与自己的 Markdown、PDF、Word、TXT 文档对话，一切在本地完成，无需联网。

## ✨ 特性

- **隐私优先** — 所有数据在本地处理，文档不上传任何云端
- **多格式支持** — Markdown、PDF、Word (.docx)、TXT
- **即用即问** — `rag index` 索引，`rag query "问题"` 回答
- **增量索引** — 新文件自动处理，已索引文件不重复处理
- **来源标注** — 每个回答附带回应的文档来源和相似度
- **轻量部署** — 基于 Ollama + ChromaDB，无需 GPU 也能运行

## 🛠 技术栈

| 组件 | 技术 |
|------|------|
| LLM 推理 | [Ollama](https://ollama.com)（支持 Qwen / DeepSeek / Llama 等） |
| 向量数据库 | [ChromaDB](https://www.trychroma.com) |
| Embedding | BGE-M3（中文语义检索） |
| 文档解析 | PyMuPDF / python-docx |
| 文本切片 | 字符切片 + 语义感知切片 |
| 前端 | CLI 命令行（Web 界面开发中） |

## ⚡ 快速开始

### 1. 环境要求

- Python 3.12+
- [Ollama](https://ollama.com/download)（已安装并运行）

### 2. 安装

```bash
git clone https://github.com/86orange/LocalRAG.git
cd LocalRAG
uv pip install -e .
```

### 3. 拉取模型

```bash
# LLM 对话模型（必选其一）
ollama pull qwen3:4b      # 推荐，轻量高效
# ollama pull qwen3:8b    # 更大参数量

# Embedding 模型（必选）
ollama pull bge-m3         # 中文语义检索
```

### 4. 开始使用

```bash
# 将文档放入 documents/ 目录，然后索引
rag index

# 向知识库提问
rag query "什么是 RAG？"

# 查看统计
rag stats
```

## 📁 项目结构

```
local_rag/
├── cli.py              # 命令行入口
├── config.py           # 全局配置（模型、参数）
├── loader/             # 文档加载（PDF、DOCX、MD、TXT）
├── chunker/            # 文本切片（字符/语义）
├── vector_store/       # ChromaDB 向量存储封装
├── qa/                 # RAG 问答链路
│   ├── chain.py        # QA Chain 构建
│   └── prompt.py       # Prompt 模板
└── utils/              # 日志、文件工具
    ├── logger.py
    └── file_utils.py
tests/                  # 单元测试
```

## 🎯 命令一览

| 命令 | 说明 |
|------|------|
| `rag index` | 扫描 documents/ 并构建向量索引 |
| `rag index --semantic` | 使用语义感知切片 |
| `rag query "问题"` | 检索并生成回答 |
| `rag query -k 10 "问题"` | 指定返回片段数 |
| `rag stats` | 查看知识库统计 |
| `rag delete ./文件路径` | 删除指定文件的索引 |

## 🔧 配置

可通过环境变量覆盖默认配置：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `LOCAL_RAG_LLM_MODEL` | `qwen3:4b` | LLM 模型名 |
| `LOCAL_RAG_EMBEDDING_MODEL` | `bge-m3` | Embedding 模型名 |
| `LOCAL_RAG_CHUNK_SIZE` | `512` | 切片最大字符数 |
| `LOCAL_RAG_CHUNK_OVERLAP` | `128` | 相邻块重叠字符数 |
| `LOCAL_RAG_TOP_K` | `5` | 检索返回片段数 |

## 📄 License

[MIT](LICENSE)
